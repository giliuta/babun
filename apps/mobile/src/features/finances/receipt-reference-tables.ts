/** ГДЕ ЖИВУТ ССЫЛКИ НА ФАЙЛ К ДЕНЬГАМ — ВСЕ МЕСТА. Файл долга пишется в
 *  `debts.receipt_url`, строка дня — в `day_extras.receipt_url`. Пока чистка
 *  спрашивала одни операции, сохранённый файл долга считался мусором и
 *  стирался при закрытии листа (найдено описью 24.09, до первой потери).
 *  Новая таблица с `receipt_url` обязана попасть сюда — сторож в тесте.
 *  Лист без React и сети: его поднимает тестовый раннер. */
export const RECEIPT_REFERENCE_TABLES = [
  "finance_transactions",
  "debts",
  "day_extras",
] as const;
