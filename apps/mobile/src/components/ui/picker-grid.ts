// Геометрия РЕШЁТКИ ВЫБОРА — одна на два набора: цвета (`ColorPicker`) и
// значки (`IconPicker`). Они живут в одной шторке под переключателем и обязаны
// стоять клетка в клетку: разъехавшийся шаг читается как два разных контрола
// вместо одного вопроса «как это выглядит в списке».
//
// КВАДРАТЫ, А НЕ КРУЖКИ, И ПЛОТНО (владелец 2026-09-10: «сделай не кругляшки,
// а квадратики — у нас всё квадратное, кнопки квадратные; и плотнее их сделай,
// но при этом чтобы разделитель был не волосина»). Раньше это была грядка из
// сорока кружков 24pt с волосяной обводкой у каждого: обводка спорила с самим
// цветом, а между кружками гулял воздух в 18pt.
//
// Разделяет плитки ЗАЗОР, а не линия: 6pt фона между квадратами видно и на
// бледной плитке, и на густой, и он не добавляет к цвету чужой пиксель.
//
// Восемь в ряду — но плитка теперь занимает КЛЕТКУ ЦЕЛИКОМ (42pt), а не сидит
// кружком 24pt в её середине. Цвет виден им самим, палец попадает без hitSlop,
// и все сорок помещаются в пять рядов — то есть в полэкрана шторки, без
// прокрутки. Шесть или пять в ряду дают плитку крупнее, но тогда решётка
// уезжает под футер и сорок цветов приходится листать.
export const PICKER_COLUMNS = 8;
export const PICKER_GAP = 6;
/** Скругление плитки — то же «квадратное» семейство, что у кнопок и полей. */
export const PICKER_RADIUS = 10;
