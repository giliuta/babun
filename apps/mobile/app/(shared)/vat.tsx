import VatScreen from "../(dashboard)/finances/vat";

// НДС ИЗ НАСТРОЕК СЧЁТА — ПОВЕРХ ПОЗВАВШЕГО, А НЕ ВНУТРЬ ВКЛАДКИ.
//
// Тот же экран, что «Финансы → НДС», но адресом в группе `(shared)` — над
// табами. Счета живут в КОРНЕВОМ стеке (`app/accounts/_layout.tsx`), и push на
// `/finances/vat` клал поверх них вторую копию таб-бара: «назад» уводил на
// календарь, а настройки счёта, из которых пришли, исчезали. Ровно тот же
// приём, которым чинили услуги, типы событий, типы объектов и категории
// (AGENTS.md → Canon Reuse 5.4).
export default function AccountVatScreen() {
  return <VatScreen />;
}
