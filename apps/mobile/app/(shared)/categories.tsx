import CategoriesScreen from "../(dashboard)/cabinet/categories";

// КАТЕГОРИИ ИЗ ЛИСТА ОПЕРАЦИИ — ПОВЕРХ ЛИСТА, А НЕ ВМЕСТО НЕГО (владелец
// 2026-09-10: «нажимаю настройки — оно открывается за шторкой… я нажимаю назад,
// и оно должно перебрасывать на ту операцию, которую я создавал»).
//
// Тот же экран, что в Кабинете, но с адресом в группе `(shared)` — над табами,
// сиблингом того, кто его позвал. Причина навигационная и та же, что у услуг и
// типов событий: `/cabinet/categories` живёт внутри ВКЛАДКИ «Кабинет», и push
// туда с экрана вкладки «Финансы» кладёт поверх него ВТОРУЮ копию табов —
// «назад» уводил на календарь, а операция со всем набранным исчезала. Здесь
// «назад» возвращает ровно на финансы, а лист операции поднимается обратно
// (`use-sheet-doorway`).
export default function OperationCategoriesScreen() {
  return <CategoriesScreen />;
}
