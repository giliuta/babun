// РЕКВИЗИТЫ ИЗ ДОКУМЕНТА — ПОВЕРХ ДОКУМЕНТА, А НЕ ВМЕСТО НЕГО.
//
// Та же страница, что в настройках финансов, но с адресом в группе
// `(shared)` — рядом с табами, сиблингом того, кто её позвал. Составитель чека
// и форма инвойса живут в КОРНЕВОМ стеке (`app/documents/*`, `app/invoices/*`),
// и прямой push на `/finances/requisites` (экран внутри вкладки «Финансы»)
// положил бы поверх них вторую копию таб-бара: «назад» увело бы на календарь,
// а набранный документ пропал бы вместе с ним. Ровно та же причина, по
// которой существует `app/(shared)/services.tsx`.
export { RequisitesScreen as default } from "@/features/companies/RequisitesScreen";
