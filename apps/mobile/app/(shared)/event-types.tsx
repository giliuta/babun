import { EventTypesScreen } from "@/features/reference/screens/EventTypesScreen";

// ТИПЫ СОБЫТИЙ ИЗ ФОРМЫ СОБЫТИЯ — ПОВЕРХ ФОРМЫ, А НЕ ВМЕСТО НЕЁ (владелец
// 2026-09-08: «захожу в типы событий через событие, нажимаю „назад" — и оно
// вылетает, а должно чётко возвращаться в событие, которое я делаю»).
//
// Тот же экран, что в Кабинете, но с адресом в группе `(shared)` — над
// табами, сиблингом того, кто его позвал. Причина навигационная, ровно та
// же, что у карточки клиента: `/cabinet/event-types` живёт внутри ВКЛАДКИ
// «Кабинет», и push туда с экрана над табами клал поверх него вторую копию
// табов — «назад» уводил на календарь, а форма со всем набранным оставалась
// висеть под ним. Здесь «назад» возвращает ровно в событие.
export default function BookEventTypesScreen() {
  return <EventTypesScreen />;
}
