# Карточка клиента — аудит 11 агентов, 2026-07-27

Полный отчёт (70 находок с путями, строками и сценариями) лежит в
`/Users/artem/.claude/projects/-Users-artem-Documents-Project-Claude-Babun/72142138-*/tasks/wpz14iirc.output`
(инвентарь серого/радиусов/попапов/языка/дат + пять линз аудита).

## Закрыто (коммиты d28d1663 · ca977a49 · d1a17cb1 · 190bd776 · ef5b0101)

- Серого нет: тиры текста = чернила с прозрачностью, капс-ярлыки чёрные, пояснения приглушены размером.
- Один радиус: `card` и `input` схлопнуты в 14, `logo` удалён, в зоне клиентов вычищено 30 литералов. Круг (999) и верх листа (24) — геометрические исключения.
- Любой выбор значения выезжает снизу (`ChoiceSheetHost`), включая кнопку связи, карты, подписи номеров.
- Язык клиента удалён из продукта (колонка в базе цела).
- Тип объекта: порядок не переставляется под пальцем (две петли обратной связи + `appendOnly`), стандарт Дом · Квартира · Офис, «+» заменён шестерёнкой в справочник.
- Зона «убрать номер» больше не залезает на кнопку связи; удаление спрашивает; массив номеров откатывается при сбое записи.
- Чипы не перекрываются зонами касания при переносе строки.
- Строка объекта — три строчки + кнопка маршрута (четыре карты).
- День рождения и даты ТО — колесо в нижнем листе (`DateWheelSheet`).
- Фасет «Тип объекта» строится из фактов, а не из мёртвого enum'а.
- Каналы связи — ключ по тенанту.

## Не закрыто, по убыванию вреда

1. **Кондиционер создаётся страницей** (`app/(dashboard)/clients/unit.tsx`, `unitId === "new"`) — паттерн, снесённый у объекта. Нужен `UnitSheet` по образцу `ObjectSheet`.
2. **«Техника = кондиционер» зашито**: `ACType`, «+ Добавить кондиционер», `ac_type ?? "split"` при чтении из базы (`repositories/clients.ts:128` сочиняет тип). Для клининга/автосервиса карточка врёт.
3. **Валюта**: карточка/список/визиты печатают € (`money.ts:formatEUR`), инвойс того же клиента — валюту тенанта.
4. **Страна**: `DEFAULT_COUNTRY = "CY"` + руками набранные 29 стран; всё остальное молча становится +357. Список должен приходить из libphonenumber.
5. **Вертикаль с онбординга никуда не идёт**: справочники не засеиваются.
6. «Записать» значит три разных вещи на одной карточке; число визитов названо дважды разными числами.
7. Оффлайн-серия правок = N операций с одним `expected_updated_at` → ложные конфликты синка.
8. Радиусы в календаре и финансах (≈380 объявлений) — за соседними сессиями.

## Правила, записанные в DESIGN-SYSTEM.md

- «Серого нет — есть чёрный разной громкости».
- «Один радиус на всё» + два геометрических исключения.
- «Создание листом, информация страницей».
- «Ожидание всегда движется» (Spinner · LoadingBar · usePullRefresh).
