# Аудит вкладки «Кабинет» — 2026-07-13

Полный аудит всех 30+ экранов кабинета (6 параллельных аудиторов: Смена, Личный
кабинет, Записи, Справочники, Компания, карта веб-эталона). Цель владельца:
«полная структура настроек, чтобы всё взаимодействовало».

## Главный диагноз

Каркас кабинета здоров: битых роутов нет, все экраны достижимы (кроме архива),
UI-примитивы единые, мутации с обработкой ошибок. Но **часть настроек — 
пустышки**: данные сохраняются, а приложение их не читает. Это и есть «не
взаимодействует».

## P0 — настройки-пустышки и деньги

| # | Находка | Файлы | Статус |
|---|---------|-------|--------|
| 1 | **Объёмные цены услуг игнорируются при записи** — buildServices берёт плоскую price, веб считает через pricePerUnit → суммы записей расходятся между платформами (ДЕНЬГИ) | `src/features/appointments/helpers.ts:48-63` vs `packages/shared/src/local/services.ts:136-156` | ✅ СДЕЛАНО |
| 2 | **Лояльность — пустышка**: уровни пишутся в `tenant_loyalty_settings`, но скидка не применяется при записи, бейджа уровня в карточке клиента нет (веб: useLoyaltyAutoApply + FinanceBlock) | `loyalty.tsx`, `AppointmentSheet.tsx`, `clients/blocks/FinanceBlock.tsx:22-24` (TODO в коде) | ✅ СДЕЛАНО |
| 3 | **Типы событий — пустышка**: пишутся в `personal_event_types`, но режим «Событие» предлагает зашитую палитру, `event_type_id` не записывается; в редакторе нет иконок и «весь день» | `event-types.tsx`, `AppointmentSheet.tsx:934-944` | ✅ СДЕЛАНО |
| 4 | **Метки (labels.tsx) — полупустышка**: `personalLabels`/`personalDefaultLabel` никто не читает; календарь красит дни через `day_cities`+`team.default_city`+цвет `cities`. Экран обещает «★ красит новые даты» — ложь | `labels.tsx`, `app/(dashboard)/index.tsx:321-349` | ✅ СДЕЛАНО (экран = библиотека меток + каскад переименования) |
| 5 | **SMS-тумблер «Отправлять автоматически» — no-op**: автоотправки на мобиле нет, флаг лишь скрывает шаблон из ручной палитры | `sms-templates.tsx:321-327` | ✅ СДЕЛАНО (подпись «Показывать в палитре SMS») |
| 6 | **«Блоки формы записи» в хабе бригады — мёртвая секция**: 9 тумблеров пишут `team.appointment_blocks`, форма записи их не читает (и веб не читает) | `teams/[id]/index.tsx:723-834` | ✅ СДЕЛАНО (секция удалена: 6 из 9 блоков в мобильной форме не существуют, поле не читает и веб; вернём вместе с поддержкой в форме) |

## P1 — нет редактирования → деструктивные обходы

| # | Находка | Файлы | Статус |
|---|---------|-------|--------|
| 7 | Шаблоны транзакций: только create+delete; нет полей payment_method/brigade_id/account_id (форма операции их ПРИМЕНЯЕТ, а экран не задаёт); shared `updateFinanceTemplate` не используется | `templates.tsx` | ✅ СДЕЛАНО |
| 8 | Категории: нельзя переименовать; обход «удалить+создать» обнуляет category_id у истории (HARD delete, SET NULL) | `categories.tsx` | ✅ СДЕЛАНО |
| 9 | Счета: нельзя переименовать/сменить бригаду (repo `updateAccount` есть, хука нет) | `accounts.tsx` | ✅ СДЕЛАНО |
| 10 | Архивные команды и мастера недостижимы: списки только active, «Вернуть из архива» есть, но дойти нельзя — архивация необратима из UI (веб показывает архив) | `teams/index.tsx`, `masters/index.tsx` | ✅ СДЕЛАНО |
| 11 | Глобальная роль мастера не задаётся нигде → пресеты прав всегда «helper» | `masters/index.tsx`, `masters/[id]/info.tsx` | ✅ СДЕЛАНО |
| 12 | Города: нет тумблера активности (меню обещает «Список и активность»), реактивация невозможна; переименование города осиротит метки бригад (имена-строки) | `cities.tsx`, `reference/queries.ts` | ✅ СДЕЛАНО |
| 13 | RefListScreen глотает ошибку загрузки → ложное «пусто» при сбое сети (страдают cities/categories/accounts/object-types) | `RefListScreen.tsx:136-138` | ✅ СДЕЛАНО |

## P2 — паритет и полнота (частично сделано, частично паркуется)

| # | Находка | Решение |
|---|---------|---------|
| 14 | «Типы объектов» — справочник-сирота: device-local MMKV, ни один экран не читает; ObjectsBlock клиента — свободный текст | ✅ СДЕЛАНО: чипы пресетов в форме объекта (хранение пока device-local) |
| 15 | calendar.tsx выставляет 4 из ~13 живых настроек: bufferMinutes/weekStart/timezone/видимое окно/scrollOpenHour имеют потребителей, но не имеют UI | ✅ ЧАСТИЧНО: буфер добавлен + сноска про командный override hideCancelled; weekStart НЕ добавлен — потребителя нет (не плодим пустышки); видимое окно/таймзона уже есть в настройках команды |
| 16 | business.tsx: нет страны/валюты/адреса/сферы/telegram/instagram (веб пишет их в tenants) | ОСТАЛОСЬ: страна/валюта/адрес/сфера/tg/insta (поля tenants готовы, чистый UI); логотип/GDPR — паркуется |
| 17 | account.tsx: нет «Выйти на других устройствах» (веб scope:"others"); нет 2FA | ✅ ЧАСТИЧНО: «Выйти на других устройствах» добавлен; 2FA — паркуется (QR-энролл) |
| 18 | Заголовки экранов ≠ пунктам меню («Бизнес» vs «Личная информация», «Шаблоны операций» vs «Шаблоны транзакций») | ✅ СДЕЛАНО |
| 19 | Герой-карта не тапабельна (ожидание: тап → профиль) | ✅ СДЕЛАНО |
| 20 | Склад: нет поиска и группировки по бригадам; позиция архивной бригады без метки | ОСТАЛОСЬ (поиск + группировка по бригадам + метка архивной бригады) |
| 21 | Сводка: нет loading/error; плитки не кликабельны | ✅ СДЕЛАНО (состояния); drill-through — паркуется |
| 22 | close-day: ручной пересчёт кассы вместо shared computeFinancials (риск дрейфа формулы); reopen без подтверждения | ОСТАЛОСЬ (деньги — делать со свежим адверсариальным ревью; сейчас формулы сверены построчно, паритет верный) |
| 23 | unclosed: не пишет аудит-лог (веб пишет logAudit) | Паркуется: на мобиле нет аудит-инфраструктуры, тащить надо целиком |
| 24 | Участники CRM-аккаунта (tenant_members/invitations, веб settings/team) — нет аналога | Паркуется: инвайты требуют серверного email-флоу |
| 25 | Матрицы прав: write-all-members из снапшота → lost update при быстрых тапах (RISK-3 задокументирован) | Паркуется (низкая частота, один владелец) |
| 26 | Выплаты (payout_percentage write-only), логин/документы/аватар мастера, удаление аккаунта (edge-fn), Тариф/Автосмс/Онлайн-запись/Интеграции | Паркуется — блокировано бэкендом/пересборкой (см. HANDOFF §3b) |

## Что признано ЗДОРОВЫМ (не трогать)

- Смена: close-day/unclosed логика кассы верна (сверено с compute.ts построчно),
  связь «Закрыть день ↔ Незакрытые» есть, оплата пишет зеркальные колонки.
- recurring.tsx — эталонный экран: полный цикл посев→напоминание→запись.
- Команды: хаб⇄счета⇄услуги⇄метки⇄календарь — двусторонне замкнуты; delete-свипы
  корректны с обеих сторон (мастер↔команда).
- Offline/инвалидации: везде корректные ключи, ошибки мутаций не глотаются.

## Итог сессии 2026-07-13

Сделано 18 из 22 актуальных пунктов (см. статусы выше), 9 коммитов
3048d9c2…e7443983, tsc=0 после каждого слайса. Все правки уехали владельцу
по Fast Refresh.

ОСТАЛОСЬ (следующая сессия):
1. #20 Склад: поиск + группировка по бригадам (веб-паритет).
2. #22 close-day → shared computeFinancials + confirm на reopen (деньги —
   с адверсариальным ревью).
3. #16 business.tsx: страна/валюта/адрес/сфера/telegram/instagram.
4. «Глубина услуг» (HANDOFF §3a): тарифы/себестоимость/цвет в редакторе
   услуг — применение тарифов при записи уже починено (#1).
Паркуется до бэкенда/решений: #23 аудит-лог, #24 участники CRM, #25
матрицы прав, #26 (billing/2FA-энролл/удаление аккаунта/выплаты).
