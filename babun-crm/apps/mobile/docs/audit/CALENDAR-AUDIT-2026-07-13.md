# Аудит календаря — 2026-07-13

Глубокий проход по всей фиче календаря (16 файлов + shared) по запросу
владельца «довести до 10/10». Применено в коммитах этого дня; ниже —
что сделано и что осознанно отложено.

## Применено

| # | Находка | Фикс |
|---|---------|------|
| 1 | Drag-перенос блока: `withSpring(0)` вёз карточку к старому слоту 300 мс → «дёрганье» после оптимистического ребейза | Сброс смещения решается в commit: перенос состоялся → мгновенно (база уже новая), нет → пружиной домой (DayView Block) |
| 2 | MonthView гонял `computeDayFinance` по 42 клеткам в каждом рендере | Мемо-карта totalsByDay (+дни только-с-extras) + `React.memo(MonthView)` + стабильные пропсы страниц в index |
| 3 | Стартовый день = часы устройства, не бизнес-TZ бригады | Однократная перепривязка к бизнес-сегодня после резолва TZ, пока пользователь не навигировал |
| 4 | Полоска событий «весь день» 8pt — фактически некликабельна | hitSlop до ~24pt |
| 5 | Тап по блоку без отклика до открытия шита | iOS-подсветка: притухание opacity с `onBegin` |
| 6 | Повторяющиеся события старше ~года исчезали (кап 365 ДНЕЙ ПРОХОДА от seed в shared expand-repeat) | Кап пройденных дней → 20k (предохранитель), смысловые лимиты (count / 365 вхождений) не тронуты |
| 7 | Открывающий скролл к scrollOpenHour промахивался (считался до layout, зум-пол поднимал высоту после) | Скролл выполняется после первого onLayout |
| 8 | Рельс показывал «00:00» внизу окна 0–24 | `24:00` |
| 9 | Крупный системный шрифт выталкивал/срезал деньги и время (fontSize 9–12 + numberOfLines) | `maxFontSizeMultiplier={1.3}` на критичных цифрах (блоки, рельс, футер, месяц) |
| 10 | `#fff`-литералы вместо `t.onAccent`; rgba-литералы акцента; тень-литерал | Токены: `t.onAccent`, `${t.accent}1a/1f`, `t.cardShadow` |
| 11 | СБ/ВС в шапке месяца — danger с opacity 0.6 (ниже AA, «приглушённый серый») | Полный danger (принцип «из чёрного, не серого») |
| 12 | Тап-мишени < 44pt: пилл режима (36), шестерёнка пикера (36) | hitSlop 6 |
| 13 | A11y: шапка дня не называла дату; заголовки дней «Списка» без роли | Дата в accessibilityLabel; `accessibilityRole="header"` |
| 14 | `monthSwipe` — дублировавший порог-свайп | Умер вместе с порогами: живой пейджер (pager.tsx) на всех видах |

## Отложено (кандидаты следующих проходов)

1. **Хаптики** (selection на тап/режим/сегодня, impact на drag-pickup,
   success на перенос) — требуют `expo-haptics` = нативный пересбор.
   Добавить в один заход с pending-ребилдом (expo-dev-client).
2. **Минутный тик перерисовывает всё дерево** — useCallback-пакет +
   `React.memo` DayColumn/Block; сперва замерить, iPhone 17 держит 60fps.
3. Pull-to-refresh на сетках День/Неделя/Месяц (сейчас только Список).
4. Кнопка «Сегодня» в шапке монтируется/уходит → сдвиг тайтла; резервировать слот.
5. Унификация дат: humanDay vs toLocaleDateString vs shared getMonthName;
   WEEKDAYS_RU скопирован 5×; addDays/isSameDay дублируют shared date-utils.
6. MiniCalendar: setState×3 в рендере (сходится, но лишние проходы); ячейки 40pt.
7. Фолбэк `getCurrentCyprusTime` игнорирует DST (жёсткий +3) — виден только у
   команд без таймзоны.
8. Короткие блоки (<24pt) — тап-мишень мала; hitSlop конфликтует с соседними
   блоками, думать через увеличение minHeight.
